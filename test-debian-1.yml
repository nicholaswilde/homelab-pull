---
- hosts: localhost
  connection: local
  become: true

  pre_tasks:
    - name: update repositories
      apt:
        update_cache: true
      changed_when: false

  tasks:
    - name: Debug
      ansible.builtin.debug:
        msg: "Running hostname file"
